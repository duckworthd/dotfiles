
# install `autojump` command
brew install autojump

# The following needs to be added to your .bash_profile or .zshrc,
#
# [[ -s `brew --prefix`/etc/autojump.sh ]] && . `brew --prefix`/etc/autojump.sh
