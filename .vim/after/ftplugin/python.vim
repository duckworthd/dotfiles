" vim's default filetype plugin for Python uses the PEP8 recommended 4 space
" tabs. I prefer 2 space tabs. This script will be run AFTER vim's default.

set tabstop=2     " number of spaces a tab counts for
set shiftwidth=2  " tab appears 2 spaces long
set softtabstop=2 " backspace deletes tabs of length 2
