; use 2-space indentation
(setq-default py-indent-offset 2)

; want to user ipython, but then code completion doesn't work :/
(setq-default py-shell-name "python")

; use pymacs for code completion
(setq-default py-load-pymacs-p t)
